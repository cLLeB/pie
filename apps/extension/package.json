{
  "name": "@pie/extension",
  "version": "0.0.1",
  "description": "PIE browser extension (MV3) — relays high-stakes integrity signals to the exam page",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pie/integrity-core": "*"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
