{
  "name": "pie",
  "version": "0.0.1",
  "private": true,
  "description": "PIE — Platform for Intelligent Examinations: browser-based proctoring with Proof-of-Authorship, privacy-first exam integrity",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present"
  }
}
