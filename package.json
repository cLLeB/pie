{
  "name": "protractor",
  "version": "0.0.1",
  "private": true,
  "description": "Browser-based proctoring with Proof-of-Authorship — privacy-first exam integrity",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present"
  }
}
